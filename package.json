{
  "name": "chek",
  "version": "1.2.5",
  "description": "Minimal utility for checking types, working with arrays and objects.",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "clean": "node ./build/clean",
    "compile": "node ./node_modules/typescript/bin/tsc -p ./src/tsconfig.json",
    "browserify": "BROWSER=true node ./node_modules/browserify/bin/cmd  ./dist/index.js -o ./dist/browser.js",
    "uglify": "node ./node_modules/uglify-js/bin/uglifyjs ./dist/browser.js --output ./dist/browser.min.js --compress --mangle",
    "typedoc": "node ./node_modules/typedoc/bin/typedoc --out ./docs ./src --options ./typedoc.json --tsconfig ./src/tsconfig.json",
    "bump": "node ./build/bump",
    "build": "npm run clean && npm run compile && npm run browserify && npm run uglify && npm run typedoc && touch ./docs/.nojekyll",
    "commit": "git add . && git commit -am 'auto commit' && git push",
    "release": "npm run build && npm run bump && npm run commit && npm publish",
    "test": "nyc mocha --opts ./src/mocha.opts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/origin1tech/chek.git"
  },
  "keywords": [
    "Helpers",
    "Utils",
    "Lodash",
    "Underscore"
  ],
  "author": "Origin1 Technologies <origin1tech@gmail.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/origin1tech/chek/issues"
  },
  "homepage": "https://github.com/origin1tech/chek#readme",
  "devDependencies": {
    "@types/chai": "^4.1.3",
    "@types/clone": "^0.1.30",
    "@types/mocha": "^5.2.0",
    "@types/node": "^8.10.17",
    "browserify": "^14.4.0",
    "chai": "^4.1.2",
    "deep-freeze": "0.0.1",
    "del": "^3.0.0",
    "mocha": "^5.2.0",
    "moment": "^2.22.1",
    "nyc": "^11.0.3",
    "ts-node": "^6.1.1",
    "tslint": "^5.10.0",
    "typedoc": "^0.7.1",
    "typescript": "^2.9.2",
    "uglify-js": "^3.0.24"
  },
  "dependencies": {
    "lodash.clone": "^4.5.0"
  },
  "nyc": {
    "include": [
      "src/**/*.ts",
      "src/**/*.tsx"
    ],
    "exclude": [
      "src/**/*.spec.ts"
    ],
    "extension": [
      ".ts",
      ".tsx"
    ],
    "require": [
      "./build/spec"
    ],
    "reporter": [
      "text",
      "html"
    ],
    "sourceMap": true,
    "instrument": true
  }
}